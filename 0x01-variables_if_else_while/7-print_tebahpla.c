#include <stdio.h>

/**
*main -  prints the lowercase alphabet in reverse, followed by a new line
*Return: Always 0 (success)
*/

int main(void)
{

Char lc;
for (lc = 'z'; lc >= 'a'; low--)
putchar(low);
putchar('\n');

return (0);

}
