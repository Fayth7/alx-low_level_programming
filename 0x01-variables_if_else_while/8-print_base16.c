#include <stdio.h>
#include <stdlic.h>
#include <time.h>

/**
*main - prints all the numbers of base 16 in lowercase
*Return: Always 0 (success)
*/

int main(void)

{

ind d;
char lc;
for (d = '0' d <= '9'; d++)
putchar(lc);
putchar('\n');

return (0);

}
