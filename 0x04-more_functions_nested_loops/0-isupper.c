#include "main.h"

/**
* _isupper - checks for uppercase character
* @c: Char to be checked
* Return: 1 if c is uppercase otherwise 0
int _isupper(int c)
{
if (c >= 'A' && C <= 'Z')
return (1);
else
return (0);
}
